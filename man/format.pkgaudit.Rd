% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pkgaudit_methods.R
\name{format.pkgaudit}
\alias{format.pkgaudit}
\alias{print.pkgaudit}
\title{Format or print a pkgaudit result}
\usage{
\method{format}{pkgaudit}(x, path = TRUE, ...)

\method{print}{pkgaudit}(x, path = TRUE, ...)
}
\arguments{
\item{x}{A \code{pkgaudit} object.}

\item{path}{Logical; if \code{TRUE} (default) include the \verb{Path:} line showing the
local filesystem location scanned, with the home directory written as \code{~}.
Set \code{FALSE} to omit the line.}

\item{...}{Ignored, for S3 compatibility.}
}
\value{
\code{format.pkgaudit()} returns a character vector of lines.
\code{print.pkgaudit()} returns \code{x} invisibly.
}
\description{
\code{format.pkgaudit()} renders the scan metadata and finding counts as a
character vector of lines; \code{print.pkgaudit()} writes those lines and returns
the object invisibly.
}
\examples{
# untrustedpkg is a small package shipped with pkgaudit to be scanned.
tarball <- system.file(
  "extdata", "untrustedpkg", "untrustedpkg_0.1.0.tar.gz",
  package = "pkgaudit"
)
result <- audit_tarball(tarball)

print(result)
print(result, path = FALSE)   # omit the local Path: line for sharing

}
\seealso{
\code{\link[=summary.pkgaudit]{summary.pkgaudit()}} for a sectioned report of the findings
themselves rather than their counts.
}
