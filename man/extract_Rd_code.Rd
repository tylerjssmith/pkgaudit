% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_Rd_code.R
\name{extract_Rd_code}
\alias{extract_Rd_code}
\title{Extract the R code from an .Rd help file}
\usage{
extract_Rd_code(path, macros = NULL)
}
\arguments{
\item{path}{Path to a single \code{.Rd} file.}

\item{macros}{Rd macros to expand while parsing, as returned by
\code{\link[tools:loadPkgRdMacros]{tools::loadPkgRdMacros()}}. Defaults to \code{NULL}, which parses the file
alone.}
}
\value{
A named list of three elements:
\describe{
\item{examples}{Length-one character string: the code from
\verb{\\examples\{\}}.}
\item{sexpr}{Length-one character string: the code from every \verb{\\Sexpr\{\}}
macro in the file, wherever it appears.}
\item{error}{\code{NULL} when the file parsed cleanly, otherwise the parser's
message. Both code strings are \code{""} when the file could not be parsed
at all, and may be \emph{incomplete} when it parsed with a warning, so a
non-\code{NULL} \code{error} means the extraction is not to be trusted as a full
account of the file's code.}
}
}
\description{
Recovers the two kinds of R code an \code{.Rd} file can carry -- the body of
\verb{\\examples\{\}} and the code inside \verb{\\Sexpr\{\}} macros -- as text suitable for
\code{base::parse(text = )}.
}
\details{
The two streams are returned separately because they run at different times.
\verb{\\examples\{\}} runs under \verb{R CMD check} and when a user calls \code{example()};
\verb{\\Sexpr\{\}} runs while the help page is built or installed, which is the
earlier and less visible of the two. Merging them would make that distinction
unrecoverable.

Both strings are \emph{line-aligned}: line N of the returned text is line N of the
\code{.Rd} file, and everything else is blank padding. Parsing with
\code{parse(text = , keep.source = TRUE)} therefore yields source references whose
line and column numbers point straight into the original file, with no offset
table to carry around. Columns are preserved the same way where the fragments
allow it.

Code is recovered from \code{tools::parse_Rd()}'s parse tree rather than by
matching text, so brace nesting, \verb{\%} comments, and the \verb{\\\%} / \verb{\\\\} / \verb{\\\{}
escapes are handled by R's own Rd parser. The extracted text is real R code:
an example written \code{cat("a\\\\nb")} in the \code{.Rd} comes back as \code{cat("a\\nb")}.

Inside \verb{\\examples\{\}}:
\itemize{
\item \verb{\dontrun{}}, \verb{\donttest{}}, \verb{\dontshow{}}, and \verb{\testonly{}} are
unwrapped and their contents included. Whether the code is reached is a
question for the caller; all four are R code shipped in the package.
\item \verb{\\dots} becomes \code{...}, so a call does not silently lose an argument.
\item Rd comments (\verb{\%} to end of line) are dropped, as they are not R code
and would not parse.
\item An inline \verb{\\Sexpr\{\}} goes to the \code{sexpr} stream and leaves a gap in
the \code{examples} stream, so \verb{h(\\Sexpr\{2+2\})} yields \code{h()} there.
}

User-defined Rd macros are expanded when \code{macros} is supplied, so a
\verb{\\Sexpr\{\}} reaching a page through a macro is recovered at the point of use,
with a source reference pointing at the page that used it. Without \code{macros},
that code is invisible and each use records an \verb{unknown macro} warning.
Scanning \verb{man/macros/} directly would not help: \code{tools::parse_Rd()} returns a
\verb{\newcommand} body as an opaque token, so the code inside it is not reachable
until the macro is expanded somewhere.
}
\section{Security considerations}{

Nothing here evaluates the code it extracts. R's Rd machinery separates
parsing from rendering: \code{tools::parse_Rd()} and \code{tools::loadPkgRdMacros()}
only read, while the \code{tools::prepare_Rd()} and \verb{tools::Rd2*()} family
evaluate \verb{\\Sexpr\{\}} as a matter of course. pkgaudit must never call the
latter, and a regression test asserts that a scan of a package whose Rd code
would write a marker file leaves no marker behind.
}

\section{Known limits}{

\verb{\\Sexpr[results=rd]} produces Rd that is itself parsed and may contain
further code; that second-order surface is not followed. No \code{stage} option is
consulted, so the \code{sexpr} stream mixes build-, install-, and render-time code
together.

\code{tools::parse_Rd()} recovers from some malformed input with a warning rather
than an error, returning a truncated tree. Whatever was recovered is still
returned, since dropping it would lose real code, but the warning is reported
in \code{error} so that a partial extraction is never mistaken for a complete one.

The \code{examples} stream is not guaranteed to parse. R never syntax-checks
\verb{\dontrun{}}: \code{tools::Rd2ex()} comments those lines out with \verb{##D}, and
\verb{R CMD check} therefore never sees them. Including that code, as this
function does, exposes \verb{\dontrun{}} blocks that are not valid R -- a stray
bracket, a sentence of prose, a mis-escaped backslash. Over a sample of 3081
\code{.Rd} files from CRAN, 5 (0.16\%) produced an \code{examples} stream that would not
parse, every one of them for that reason; no \code{sexpr} stream failed. Since the
two streams are each assembled whole, one broken \verb{\dontrun{}} costs the
valid example code in the same file, which is worth weighing before this is
wired into a scan.
}

\examples{
\dontrun{
res <- extract_Rd_code("man/audit_package.Rd")
cat(res$examples)
parse(text = res$examples, keep.source = TRUE)
parse(text = res$sexpr)
}
}
