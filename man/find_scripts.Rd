% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_scripts.R
\name{find_scripts}
\alias{find_scripts}
\title{Find R scripts executed at install/load time}
\usage{
find_scripts(pkg)
}
\arguments{
\item{pkg}{Path to the root of the package being audited.}
}
\value{
A character vector of absolute paths to R scripts. Empty if none.
}
\description{
Returns the R source files that R evaluates when a source package is
installed or its namespace is loaded: the top level of \verb{R/}, \verb{R/unix/}, and
\verb{R/windows/}, plus \code{src/install.libs.R} when present. Subdirectories of \verb{R/}
other than \verb{unix/} and \verb{windows/} are not processed by R and are excluded.
}
\details{
The \code{list.files()} calls are intentionally not wrapped in \code{tryCatch()}: a
failure to list the package's own directories is unrecoverable, so it
propagates and aborts the audit (see \code{\link[=audit_package]{audit_package()}}).
}
\keyword{internal}
