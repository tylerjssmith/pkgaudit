% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_matches.R
\name{find_matches}
\alias{find_matches}
\title{Find security-relevant matches in shell code}
\usage{
find_matches(lines, match_rules, file_context)
}
\arguments{
\item{lines}{Character vector of the file's lines, as returned by
\code{\link[=read_code]{read_code()}}.}

\item{match_rules}{Data frame of match rules (\code{rules$matches} from
\code{\link[=load_rules]{load_rules()}}), with columns \code{name}, \code{regex}, \code{message}, and \code{attck}.}

\item{file_context}{Package-root-relative path of the file, carried through
for joining to the file-contexts table.}
}
\value{
A list with two data frames:
\describe{
\item{matches}{Data frame with columns \code{rule} (the matching rule's
name), \code{file_context}, \code{line_number}, \code{column_number}, \code{message},
\code{attck}. The phase columns are not set here; \code{\link[=audit_package]{audit_package()}} attaches
them from the file context the file belongs to.}
\item{errors}{Data frame with columns \code{step}, \code{file_context}, \code{rule},
\code{message}.}
}
}
\description{
Finds matches -- regular-expression matches in code R hands to a shell or to
make, whether it is a file (e.g., \code{configure}, \code{src/Makevars}) or a shell
chunk in a vignette.
}
\details{
Each rule's regular expression is evaluated against every line with
\code{base::gregexpr(perl = TRUE)}, case-sensitively; a line matched more than once
yields one row per match. A failing expression is recorded in \code{errors} and the
scan moves on.

Matching text is less precise than matching a parse tree: a match has no
syntax behind it, so one inside a comment, a quoted string, or a branch that
never runs is reported the same as a live command. Findings are candidates for
review, not confirmed behavior.

Reading the file, and the limits protecting against hostile input, are
\code{\link[=read_code]{read_code()}}'s responsibility.
}
\keyword{internal}
