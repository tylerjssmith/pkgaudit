% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage.R
\name{build_coverage}
\alias{build_coverage}
\title{Describe how well every file in a package was read}
\usage{
build_coverage(
  path,
  found,
  file_context_rules,
  scanned = character(0L),
  errors = .empty_errors()
)
}
\arguments{
\item{path}{Path to the package root.}

\item{found}{The \code{file_contexts} frame from \code{\link[=find_file_contexts]{find_file_contexts()}}, carrying
every rule match, reporting or not.}

\item{file_context_rules}{\code{rules$file_contexts} from \code{\link[=load_rules]{load_rules()}}.}

\item{scanned}{Named character vector of the rule \code{type} each scanned file
was read as, named by package-root-relative path. The type, not the
extension, is what a scanned file's language comes from: \code{configure} has no
extension but is read as shell.}

\item{errors}{The scan's \code{errors} frame, which is what identifies the files
pkgaudit tried to read and could not.}
}
\value{
A data frame with the columns \code{\link[=audit_package]{audit_package()}} documents for
\code{coverage}, without the phase columns; \code{\link[=audit_package]{audit_package()}} attaches those.
}
\description{
Builds the \code{coverage} data frame: one row per file the package carries that
is, or could be, code, and one per code span where a file yields several,
recording whether pkgaudit parsed it, matched it as text, could hand it to
another tool, or did not examine it at all.
}
\section{Security considerations}{

The frame is built by walking the tree rather than by asking the rules what
they matched, so a file in a location no rule anticipates still gets a row.
That is the point: an unknown-unknown becomes a known-unknown, and a clean
scan becomes falsifiable. What a name has to say to earn a row is
\code{.in_scope()}.

A symlink is recorded and never read. \code{list.files()} descends into symlinked
directories and a read would follow the link out of the package, so every
ancestor component is tested, as \code{\link[=hash_manifest]{hash_manifest()}} does.
}

\keyword{internal}
