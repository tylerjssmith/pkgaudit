% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segments.R
\name{new_segment}
\alias{new_segment}
\title{Build one code segment}
\usage{
new_segment(
  language,
  lines,
  file_context,
  context = NA_character_,
  file_rule = NA_character_,
  code_contexts = NULL,
  guarded_lines = integer(0L)
)
}
\arguments{
\item{language}{The language of the code, which selects the analyzer.}

\item{lines}{Character vector of the segment's lines, blank-padded to the
line numbers they occupy in the file so a finding's line is the real one.}

\item{file_context}{Package-root-relative path of the file it came from.}

\item{context}{The segment's label, matched by a \code{kind: segment} code-context
rule, or \code{NA} where it has none. It is the only record of which part of a
help file the code came from.}

\item{file_rule}{The file-context rule that claimed the file.}

\item{code_contexts}{The code-context rules that can apply, from the source.}

\item{guarded_lines}{Integer vector of line numbers whose code ships but the
lifecycle does not run -- a \verb{\dontrun{}} block, or a chunk marked
\code{eval=FALSE}. Phases still come from the context, so they are an upper
bound.}
}
\value{
A \code{pkgaudit_segment} object.
}
\description{
A contiguous, line-aligned run of code in one language, classed by that
language so \code{\link[=analyze_segment]{analyze_segment()}} can dispatch on it. An \code{\link[=extract_segments]{extract_segments()}}
method returns these.
}
\examples{
seg <- new_segment(language = "R", lines = c("", "system('id')"),
                   file_context = "R/f.R")
class(seg)
}
\seealso{
\code{\link[=extract_segments]{extract_segments()}}, whose methods return these.
}
