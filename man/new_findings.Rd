% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segments.R
\name{new_findings}
\alias{new_findings}
\title{Build the return value of an \code{\link[=analyze_segment]{analyze_segment()}} method}
\usage{
new_findings(
  patterns = NULL,
  matches = NULL,
  coverage = NULL,
  errors = .empty_errors()
)
}
\arguments{
\item{patterns, matches, coverage}{Data frames with the columns
\code{\link[=audit_package]{audit_package()}} documents, less the phase columns, or \code{NULL} for none.
Extra columns are dropped; the phases are resolved later, from context.}

\item{errors}{Data frame with columns \code{step}, \code{file_context}, \code{rule} and
\code{message}.}
}
\value{
A list of the four frames, each conformed to its canonical columns.
}
\description{
Each frame is reduced to its canonical columns and an omitted one is empty.
\code{rbind()} accepts a stray column silently, so conforming here is what keeps a
malformed frame out of the result.
}
\examples{
# An analyzer that found nothing still returns all four frames.
str(new_findings(), max.level = 1)
}
\seealso{
\code{\link[=analyze_segment]{analyze_segment()}}, whose methods return this.
}
