% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_tar.R
\name{tar_entries}
\alias{tar_entries}
\title{List tar entries with type and link target}
\usage{
tar_entries(
  tarfile,
  max_entries = 100000L,
  max_bytes = 2 * 1024^3,
  max_ratio = 256,
  chunk = 1048576L
)
}
\arguments{
\item{tarfile}{Path to a \code{.tar} or \code{.tar.gz} archive.}

\item{max_entries}{Maximum number of entries to read before failing closed.
Default 100,000.}

\item{max_bytes}{Maximum uncompressed bytes to read before failing closed.
Default 2 GB. Raise for ecosystems with larger artifacts, e.g. Bioconductor
annotation and experiment-data packages.}

\item{max_ratio}{Maximum uncompressed:compressed ratio before failing closed,
or \code{Inf} to disable. Targets decompression bombs, which are characterised
by extreme ratios rather than absolute size. Default 256, well under the
~1032:1 ceiling of a single gzip layer.}

\item{chunk}{Bytes read per call when skipping entry data. Bounds peak
allocation so a huge declared size cannot force one huge read.}
}
\value{
A data frame with \code{name}, \code{type}, \code{linkname}, and \code{size}. \code{type} is
one of \code{"file"}, \code{"hardlink"}, \code{"symlink"}, \code{"dir"}, \code{"other"}.
}
\description{
List tar entries with type and link target
}
\details{
\code{utils::untar(list = TRUE)} returns names only, so it cannot distinguish a
symlink entry from a regular file. This reads the tar headers directly to
recover each entry's typeflag and linkname, which are used to refuse link
entries before extracting an untrusted archive.

Reads headers only: entry data is skipped, never written to disk. Note that
reaching entry N's header still requires decompressing everything before it.
Reads gzip and uncompressed tar via \code{\link[base:gzfile]{base::gzfile()}}, which would silently
decompress bzip2, xz and zstd as well, so those streams -- and compress --
are refused first, by magic bytes rather than filename. Only gzip's ~1032:1
per-layer ceiling keeps \code{max_ratio} a meaningful decompression-bomb bound;
xz can compress far past it.
}
\keyword{internal}
