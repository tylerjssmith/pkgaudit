% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_rules.R
\name{verify_db}
\alias{verify_db}
\title{Verify the integrity of the pkgaudit rules database}
\usage{
verify_db(db_path = .db_path(), sig_path = .sig_path(), pub_path = .pub_path())
}
\arguments{
\item{db_path}{Path to the SQLite rules database. Defaults to the bundled
database shipped with the package.}

\item{sig_path}{Path to the base64-encoded signature file. Defaults to the
bundled signature.}

\item{pub_path}{Path to the PEM-encoded public key. Defaults to the bundled
public key.}
}
\value{
\code{TRUE} invisibly if verification succeeds. Stops with an error if
verification fails.
}
\description{
Verifies the cryptographic signature of the rules database against the
bundled public key. Called automatically by \code{\link[=load_rules]{load_rules()}} before any
rules are returned.
}
\details{
The rules database is the primary attack surface of pkgaudit: an adversary
who can modify the database can silently remove detection for specific
patterns. Signature verification using a public key bundled at package
installation time ensures that a modified database is detected before any
analysis runs. The public key is a static file in \verb{inst/db/} that is itself
part of the signed CRAN package.
}
\examples{
\dontrun{
verify_db()
}

}
