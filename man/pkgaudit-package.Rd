% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pkgaudit-package.R
\docType{package}
\name{pkgaudit-package}
\alias{pkgaudit}
\alias{pkgaudit-package}
\title{pkgaudit: Static Analysis Security Testing (SAST) for R Packages}
\description{
A static analysis tool that flags security-relevant files and code in R source packages for human review without executing anything it scans. Models package lifecycle execution semantics -- it reports not just what a package does, but when it runs, so code that executes on install or load is distinguishable from code that runs only when called. Reads every surface a package can execute, including 'configure' scripts, help-file macros, and vignettes, and states what it could not read, so a clean result can be checked rather than trusted. Detection rules are data, shipped in a versioned and hash-verified 'SQLite' database.
}
\details{
The main entry points are \code{\link[=audit_package]{audit_package()}} and \code{\link[=audit_tarball]{audit_tarball()}}, which scan
a package source directory and tarball, respectively, and return a
\code{\link[=new_pkgaudit]{new_pkgaudit()}} object holding five result data frames (file_contexts,
patterns, matches, coverage, errors) plus scan metadata. Its
\link[=format.pkgaudit]{format()} and \link[=print.pkgaudit]{print()} methods render the
metadata and finding counts.
}
\seealso{
\code{\link[=audit_package]{audit_package()}} and \code{\link[=audit_tarball]{audit_tarball()}} for the scan entry points;
\code{\link[=print.pkgaudit]{print.pkgaudit()}} for the rendered finding counts.
}
\author{
\strong{Maintainer}: Tyler Smith \email{tylerjssmith@gmail.com} (\href{https://orcid.org/0000-0003-4692-2206}{ORCID})

Authors:
\itemize{
  \item Tyler Smith \email{tylerjssmith@gmail.com} (\href{https://orcid.org/0000-0003-4692-2206}{ORCID})
}

}
\keyword{internal}
