% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emit_sarif.R
\name{emit_sarif}
\alias{emit_sarif}
\title{Render a scan as SARIF}
\usage{
emit_sarif(object, pretty = TRUE)
}
\arguments{
\item{object}{A \code{pkgaudit} object.}

\item{pretty}{Logical; if \code{TRUE} (default) indent the JSON for reading.}
}
\value{
A length-one character vector holding the SARIF document. Nothing is
written; \code{writeLines()} it where you want it.
}
\description{
Renders a \code{pkgaudit} object as a SARIF 2.1.0 document, the format code
scanning tools publish results in, so a scan can be read in an editor or
loaded by any SARIF consumer.
}
\details{
Every finding in \code{file_contexts}, \code{patterns} and \code{matches} becomes a result,
located by the path and, where there is one, the line and column. Rule ids
are namespaced by the kind of rule -- \code{pattern/curl}, \code{match/curl},
\code{file/configure} -- because a rule name is unique only within its kind.

\code{level} is \code{note} for every result: pkgaudit does not rank findings, so
nothing is mapped onto SARIF's severity field. When a finding's code
executes is carried in \code{properties.phases}, and a \code{note} is never a claim
that a finding is minor.

\code{partialFingerprints} identifies a finding by its rule, its file, the code
context it sits in, and the text of the line -- not by line number, which
shifts whenever anything above it is edited. Two occurrences a consumer could
not otherwise tell apart are numbered, since a fingerprint repeated within a
run makes several findings read as one. The \code{coverage} frame becomes the
\code{artifacts} array, so a consumer can see which files were never read, and
\code{errors} become execution notifications on the invocation.

Requires jsonlite, a suggested dependency, since only this function needs a
JSON writer.
}
\examples{
\dontshow{if (requireNamespace("jsonlite", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# untrustedpkg is a small package shipped with pkgaudit to be scanned.
tarball <- system.file(
  "extdata", "untrustedpkg", "untrustedpkg_0.1.0.tar.gz",
  package = "pkgaudit"
)
result <- audit_tarball(tarball)

writeLines(emit_sarif(result), file.path(tempdir(), "pkgaudit.sarif"))
\dontshow{\}) # examplesIf}
}
