% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pkgaudit.R
\name{new_pkgaudit}
\alias{new_pkgaudit}
\title{Construct a pkgaudit object}
\usage{
new_pkgaudit(file_contexts, patterns, matches, coverage, errors, metadata)
}
\arguments{
\item{file_contexts, patterns, matches, coverage, errors}{Data frames with the
columns documented in \code{\link[=audit_package]{audit_package()}}.}

\item{metadata}{Named list with the fields documented in \code{\link[=audit_package]{audit_package()}},
each a length-one value of the expected type.}
}
\value{
A \code{pkgaudit} object: a named list of \code{file_contexts}, \code{patterns},
\code{matches}, \code{coverage}, \code{errors}, and \code{metadata}.
}
\description{
Assembles the five result data frames and a metadata list into a validated
\code{pkgaudit} S3 object. \code{\link[=audit_package]{audit_package()}} calls this at the end of a scan. It
is also used to construct objects directly (e.g., in tests).
}
\keyword{internal}
