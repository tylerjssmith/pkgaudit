% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_indirect.R
\name{find_indirect}
\alias{find_indirect}
\title{Find calls made through a function's name}
\usage{
find_indirect(tree, pattern_rules, file_context)
}
\arguments{
\item{tree}{The \code{xml_document} parse tree for one segment (from
\code{\link[=parse_code]{parse_code()}}).}

\item{pattern_rules}{Data frame of pattern rules (\code{rules$patterns} from
\code{\link[=load_rules]{load_rules()}}). Only the \code{functions} column decides what is found; the
matched rule supplies the row's \code{rule}, \code{message}, and \code{attck}.}

\item{file_context}{Package-root-relative path, carried through for joining.}
}
\value{
A list with two data frames, shaped exactly as \code{\link[=find_patterns]{find_patterns()}}
returns them so the two can be combined:
\describe{
\item{patterns}{The columns \code{\link[=find_patterns]{find_patterns()}} produces, plus \code{indirect},
which is \code{TRUE} on every row. Carries the matched nodes as a \code{"nodes"}
attribute, aligned to the rows.}
\item{errors}{Data frame with columns \code{step}, \code{file_context}, \code{rule},
\code{message}.}
}
}
\description{
Finds \code{do.call("system", ...)} and its siblings: a call to a function named
by a string literal, which no pattern rule's XPath can see because the target
is not a call site.
}
\details{
A finding is reported under the rule that owns the name, not under a rule of
its own, so filtering for \code{rule == "system"} returns every call to \code{system}
however it was spelled, and the \code{indirect} column says which is which. The
line and column point at the string literal rather than at \code{do.call}, since
the literal is what a reviewer needs to look at.
}
\section{Security considerations}{

A name is claimed by a rule only if calling it bare would have matched that
rule's own XPath, which \code{inst/scripts/build_db.R} verifies when the database
is built. An indirect finding therefore can never be attributed to a rule
that would not have reported the direct call.

A name the rules do not declare is not reported, so this under-covers rather
than guesses -- as does a target assembled at runtime, such as
\code{do.call(paste0("sys", "tem"), ...)}, which carries no literal to read.
}

\keyword{internal}
